# Protect admin directory
<FilesMatch "\.(php)$">
    Order allow,deny
    Allow from all
</FilesMatch>

# Disable directory browsing
Options -Indexes

# Protect sensitive files
<FilesMatch "^\.">
    Order allow,deny
    Deny from all
</FilesMatch>
